#!/bin/bash

# start: Starts an existing (stopped) container.
# -a: Attaches to the container's STDOUT/STDERR.
# -i: Keeps STDIN open (for interactive use, like a shell).

docker start -ai micro2024-container
